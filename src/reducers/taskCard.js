const taskCardReducer = (state, action) => {
    switch (action.type) {
        // eslint-disable-next-line no-undef
        case 'NEW_TASK':
            debugger
            return state
        default:
            return state
    }
}

export default taskCardReducer;